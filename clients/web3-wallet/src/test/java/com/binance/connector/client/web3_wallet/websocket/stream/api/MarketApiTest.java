/*
 * Binance Web3 WebSocket Streams
 * Real-time on-chain data streaming over WebSocket for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.websocket.stream.api;

import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.websocket.dtos.RequestWrapperDTO;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueueWrapper;
import com.binance.connector.client.web3.common.ApiClient;
import com.binance.connector.client.web3.common.dtos.GetWebSocketAuthTokenResponse;
import com.binance.connector.client.web3.common.dtos.GetWebSocketAuthTokenResponseData;
import com.binance.connector.client.web3.common.websocket.adapter.stream.Web3StreamConnectionWrapper;
import com.binance.connector.client.web3.common.websocket.configuration.Web3WebSocketClientConfiguration;
import com.binance.connector.client.web3_wallet.websocket.stream.model.*;
import org.eclipse.jetty.websocket.api.RemoteEndpoint;
import org.eclipse.jetty.websocket.api.Session;
import org.eclipse.jetty.websocket.client.WebSocketClient;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;
import org.skyscreamer.jsonassert.JSONAssert;

import java.io.IOException;
import java.net.URISyntaxException;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.HashMap;
import java.util.Set;
import java.util.concurrent.CompletableFuture;

/**
 * API tests for MarketApi
 */
public class MarketApiTest {

    private MarketApi api;
    private Web3StreamConnectionWrapper connectionSpy;
    private Session sessionMock;

    @BeforeEach
    public void initApiClient() throws Exception {
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("apiSecret");
        Web3WebSocketClientConfiguration clientConfiguration =
                new Web3WebSocketClientConfiguration();
        clientConfiguration.setAutoLogon(false);
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);
        clientConfiguration.setUrl("wss://localhost:8080");

        WebSocketClient webSocketClient = Mockito.mock(WebSocketClient.class);
        CompletableFuture<Session> sessionCompletableFuture = new CompletableFuture<>();
        Mockito.doReturn(sessionCompletableFuture)
                .when(webSocketClient)
                .connect(Mockito.any(), Mockito.any(), Mockito.any());
        sessionMock = Mockito.mock(Session.class);

        RemoteEndpoint remoteEndpointMock = Mockito.mock(RemoteEndpoint.class);
        Mockito.doReturn(remoteEndpointMock).when(sessionMock).getRemote();
        sessionCompletableFuture.complete(sessionMock);

        ApiClient httpClient = Mockito.mock(ApiClient.class);

        GetWebSocketAuthTokenResponseData getWebSocketAuthTokenResponseData = new GetWebSocketAuthTokenResponseData();
        getWebSocketAuthTokenResponseData.setToken("token123456");

        GetWebSocketAuthTokenResponse getWebSocketAuthTokenResponse = new GetWebSocketAuthTokenResponse();
        getWebSocketAuthTokenResponse.setData(getWebSocketAuthTokenResponseData);

        ApiResponse<GetWebSocketAuthTokenResponse> getWebSocketAuthTokenResponseApiResponse = new ApiResponse<>(200, new HashMap<>(), getWebSocketAuthTokenResponse);

        Mockito.doReturn(getWebSocketAuthTokenResponseApiResponse).when(httpClient).execute(Mockito.any(), Mockito.any());

        Web3StreamConnectionWrapper connectionWrapper =
                new Web3StreamConnectionWrapper(clientConfiguration, webSocketClient, httpClient);
        connectionSpy = Mockito.spy(connectionWrapper);
        Mockito.doNothing().when(connectionSpy).setUserAgent(Mockito.anyString());
        Mockito.doReturn(1736393892000L).when(connectionSpy).getTimestamp();
        connectionSpy.connect();
        MarketApi apiClient = new MarketApi(connectionSpy);
        api = Mockito.spy(apiClient);
        Mockito.doReturn("eaf3292c-64b6-4c04-ad4f-4ca2608b42b4").when(api).getRequestID();
    }

    /**
     * Candlestick Stream
     *
     * <p>Push token candlestick (K-line) data in real time. The current (unclosed) candle keeps
     * updating as new trades occur. Update Speed: Real-time
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void candlestickStreamTest() throws ApiException, URISyntaxException, IOException {
        CandlestickStreamRequest candlestickStreamRequest = new CandlestickStreamRequest();
        candlestickStreamRequest.id("e9d6b4349871b40611412680b3445fac");
        candlestickStreamRequest.bar("1s");
        candlestickStreamRequest.chainId("CT_501");
        candlestickStreamRequest.contractAddress("C3DwDjT17gDvvCYC2nsdGHxDHVmQRdhKfpAdqQ29pump");

        StreamBlockingQueueWrapper<CandlestickStreamResponse> response =
                api.candlestickStream(candlestickStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, CandlestickStreamResponse>>
                callArgumentCaptor = ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, CandlestickStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();

        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketApiTest.class.getResource(
                        "/expected/stream/MarketApi/w3w@onchainos@pub@candles@bar@chainId@contractAddress-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Market Stats Stream
     *
     * <p>Push token market data. Rate-limited to at most once per second per token: if a
     * token&#39;s data changes within a 1-second window, the latest snapshot is pushed once
     * (multiple changes are coalesced into the latest value); if there is no change, nothing is
     * pushed. Update Speed: Real-time
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void marketStatsStreamTest() throws ApiException, URISyntaxException, IOException {
        MarketStatsStreamRequest marketStatsStreamRequest = new MarketStatsStreamRequest();
        marketStatsStreamRequest.id("e9d6b4349871b40611412680b3445fac");
        marketStatsStreamRequest.chainId("CT_501");
        marketStatsStreamRequest.contractAddress("C3DwDjT17gDvvCYC2nsdGHxDHVmQRdhKfpAdqQ29pump");

        StreamBlockingQueueWrapper<MarketStatsStreamResponse> response =
                api.marketStatsStream(marketStatsStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, MarketStatsStreamResponse>>
                callArgumentCaptor = ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, MarketStatsStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();

        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketApiTest.class.getResource(
                        "/expected/stream/MarketApi/w3w@onchainos@pub@tx-data@chainId@contractAddress-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Price Stream
     *
     * <p>Push token price updates. Triggered every time a token&#39;s price changes. Update Speed:
     * Real-time
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void priceStreamTest() throws ApiException, URISyntaxException, IOException {
        PriceStreamRequest priceStreamRequest = new PriceStreamRequest();
        priceStreamRequest.id("e9d6b4349871b40611412680b3445fac");
        priceStreamRequest.chainId("CT_501");
        priceStreamRequest.contractAddress("C3DwDjT17gDvvCYC2nsdGHxDHVmQRdhKfpAdqQ29pump");

        StreamBlockingQueueWrapper<PriceStreamResponse> response =
                api.priceStream(priceStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, PriceStreamResponse>> callArgumentCaptor =
                ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, PriceStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();

        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketApiTest.class.getResource(
                        "/expected/stream/MarketApi/w3w@onchainos@pub@price@chainId@contractAddress-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Real-Time Trades Stream
     *
     * <p>Push real-time token trade history. Triggered on every new transaction. Update Speed:
     * Real-time
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void realTimeTradesStreamTest() throws ApiException, URISyntaxException, IOException {
        RealTimeTradesStreamRequest realTimeTradesStreamRequest = new RealTimeTradesStreamRequest();
        realTimeTradesStreamRequest.id("e9d6b4349871b40611412680b3445fac");
        realTimeTradesStreamRequest.chainId("CT_501");
        realTimeTradesStreamRequest.contractAddress("C3DwDjT17gDvvCYC2nsdGHxDHVmQRdhKfpAdqQ29pump");

        StreamBlockingQueueWrapper<RealTimeTradesStreamResponse> response =
                api.realTimeTradesStream(realTimeTradesStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, RealTimeTradesStreamResponse>>
                callArgumentCaptor = ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, RealTimeTradesStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();

        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketApiTest.class.getResource(
                        "/expected/stream/MarketApi/w3w@onchainos@pub@tx@chainId@contractAddress-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }
}
