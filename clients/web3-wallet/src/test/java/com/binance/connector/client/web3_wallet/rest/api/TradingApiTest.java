/*
 * Binance Web3 Wallet REST API
 * Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.rest.api;

import com.binance.connector.client.web3.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.web3.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.web3_wallet.rest.model.*;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

import static org.junit.Assert.assertEquals;

/**
 * API tests for TradingApi
 */
public class TradingApiTest {

    private TradingApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new TradingApi(apiClientSpy);
    }

    /**
     * Build Swap Transaction
     *
     * <p>Build the on-chain swap calldata for a previously quoted route. The request is matched
     * against the cached quote by &#x60;quoteId&#x60; (TTL ~30s); if the entry has expired,
     * &#x60;QUOTE_EXPIRED&#x60; (40401) is returned, and if the request parameters disagree with
     * the cached quote, &#x60;SWAP_QUOTE_MISMATCH&#x60; (40462) is returned.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void buildSwapTransactionTest() throws ApiException, CryptoException {
        String binanceChainId = "56";
        String amount = "1000000";
        String fromTokenAddress = "0x55d398326f99059fF775485246999027B3197955";
        String toTokenAddress = "0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d";
        String slippagePercent = "0.5";
        String userWalletAddress = "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045";
        String quoteId = "a1b2c3d4e5f64a8b9c0d1e2f3a4b5c6d";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String approveTransaction = "false";
        String approveAmount = "1000000";
        String gasLimit = "200000";
        String gasLevel = "average";
        String priceImpactProtectionPercent = "90";
        String autoSlippage = "false";
        String maxAutoSlippagePercent = "3";
        String computeUnitLimit = "1400000";
        String computeUnitPrice = "1000";
        String tips = "0.001";

        TradingApi.BuildSwapTransactionRequest request =
                new TradingApi.BuildSwapTransactionRequest(
                        binanceChainId,
                        amount,
                        fromTokenAddress,
                        toTokenAddress,
                        slippagePercent,
                        userWalletAddress,
                        quoteId);
        ApiResponse<BuildSwapTransactionResponse> response = api.buildSwapTransaction(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=56&amount=1000000&fromTokenAddress=0x55d398326f99059fF775485246999027B3197955&toTokenAddress=0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d&slippagePercent=0.5&userWalletAddress=0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045&quoteId=a1b2c3d4e5f64a8b9c0d1e2f3a4b5c6d&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "fbb6ec18ac54ed188292b0a8f4e42b959c6e32bba96cbca842f7fd4cc740357d",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/aggregator/swap", actualRequest.url().encodedPath());
    }

    /**
     * Get Aggregated Quote
     *
     * <p>Query multiple DEX vendors in parallel and return the priced routes sorted by
     * &#x60;toTokenAmount&#x60; (descending). Each route carries an independent &#x60;quoteId&#x60;
     * (TTL ~30s) that the swap endpoint consumes to construct calldata for the chosen route.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getAggregatedQuoteTest() throws ApiException, CryptoException {
        String binanceChainId = "56";
        String amount = "1000000";
        String fromTokenAddress = "0x55d398326f99059fF775485246999027B3197955";
        String toTokenAddress = "0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        TradingApi.GetAggregatedQuoteRequest request =
                new TradingApi.GetAggregatedQuoteRequest(
                        binanceChainId, amount, fromTokenAddress, toTokenAddress);
        ApiResponse<GetAggregatedQuoteResponse> response = api.getAggregatedQuote(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=56&amount=1000000&fromTokenAddress=0x55d398326f99059fF775485246999027B3197955&toTokenAddress=0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "08d567b67ea58def6636f12bfc3039029d7a7a516f67dc2606f80256707ec97d", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/aggregator/quote", actualRequest.url().encodedPath());
    }

    /**
     * Get Aggregator Supported Chains
     *
     * <p>Return blockchain networks supported by the DEX aggregator. The supported list is
     * dynamically configured server-side and may change over time. Pass &#x60;binanceChainId&#x60;
     * to filter to a single chain; omit it to get the full list.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getAggregatorSupportedChainsTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String binanceChainId = "56";

        TradingApi.GetAggregatorSupportedChainsRequest request =
                new TradingApi.GetAggregatorSupportedChainsRequest();
        ApiResponse<GetAggregatorSupportedChainsResponse> response =
                api.getAggregatorSupportedChains(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/aggregator/supported/chain", actualRequest.url().encodedPath());
    }

    /**
     * Get ERC-20 Approve Transaction
     *
     * <p>Build the on-chain transaction data needed to approve the DEX router to spend a user&#39;s
     * ERC-20 token before a swap. Calldata is encoded per the ERC-20 ABI standard
     * (&#x60;approve()&#x60; selector + spender + amount).
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getErc20ApproveTransactionTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0xdAC17F958D2ee523a2206206994597C13D831ec7";
        String approveAmount = "1000000";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        TradingApi.GetErc20ApproveTransactionRequest request =
                new TradingApi.GetErc20ApproveTransactionRequest(
                        binanceChainId, tokenContractAddress, approveAmount);
        ApiResponse<GetErc20ApproveTransactionResponse> response =
                api.getErc20ApproveTransaction(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0xdAC17F958D2ee523a2206206994597C13D831ec7&approveAmount=1000000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "6f4ed5825add3cc16ec2f56703d5af073e7ad7bb0c1056c95f66d21c7ad041fb",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/aggregator/approve-transaction", actualRequest.url().encodedPath());
    }

    /**
     * Get Transaction Status
     *
     * <p>Look up the on-chain status of a DEX swap by &#x60;binanceChainId&#x60; +
     * &#x60;txHash&#x60;. Response semantics: - Transaction not found: &#x60;data&#x60; is
     * &#x60;null&#x60; (not HTTP 404). - Transaction failed: &#x60;status&#x3D;failed&#x60; with
     * &#x60;errorMsg&#x60;; aggregator business fields (&#x60;txType&#x60;, &#x60;dexRouter&#x60;,
     * &#x60;fromTokenDetails&#x60;, &#x60;toTokenDetails&#x60;) are &#x60;null&#x60;. - Transaction
     * succeeded: &#x60;status&#x3D;success&#x60; with full token details when an aggregator event
     * is present.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTransactionStatusTest() throws ApiException, CryptoException {
        String binanceChainId = "56";
        String txHash = "0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        TradingApi.GetTransactionStatusRequest request =
                new TradingApi.GetTransactionStatusRequest(binanceChainId, txHash);
        ApiResponse<GetTransactionStatusResponse> response = api.getTransactionStatus(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=56&txHash=0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "293e34ace067e539f71ddf6191451d58c58edfd6674d5f68a692509b6d79c418",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/aggregator/history", actualRequest.url().encodedPath());
    }
}
