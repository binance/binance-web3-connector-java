/*
 * Binance Web3 Wallet REST API
 * Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.binance.connector.client.web3_wallet.rest.api;

import com.binance.connector.client.web3.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.web3.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.web3_wallet.rest.model.*;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

import java.util.ArrayList;

import static org.junit.Assert.assertEquals;

/**
 * API tests for WalletApi
 */
public class WalletApiTest {

    private WalletApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);


        HmacSignatureGenerator signatureGenerator = new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication = new SignatureAuthentication(signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy).when(factoryMock).getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);


        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new WalletApi(apiClientSpy);
    }

    /**
     * Get All Token Balances by Address
     * <p>
     * Return all token balances held by an address across one or more chains, with pagination support. Set &#x60;excludeRiskToken&#x3D;true&#x60; to filter out airdrop-risk and honeypot tokens (honeypot detection currently applies only to ETH / BSC / SOL / BASE).
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getAllTokenBalancesByAddressTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String address = "0xed0c6079229e2d407672a117c22b62064f4a4312";
        String chains = "1,56";
        Boolean excludeRiskToken = true;
        Long page = 1L;
        Integer pageSize = 20;

        WalletApi.GetAllTokenBalancesByAddressRequest request = new WalletApi.GetAllTokenBalancesByAddressRequest();
        ApiResponse<GetAllTokenBalancesByAddressResponse> response = api.getAllTokenBalancesByAddress(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c|" + actualRequest.url().queryParameter("signature"));
        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "/api/v1/dex/balance/all-token-balances-by-address|" + actualRequest.url().encodedPath());
        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/balance/all-token-balances-by-address", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Balances by Address
     * <p>
     * Return token balances for a specific list of (chain, contract) pairs. Up to 20 entries per request. Set &#x60;excludeRiskToken&#x3D;\&quot;0\&quot;&#x60; to exclude risk-flagged tokens (default), or &#x60;\&quot;1\&quot;&#x60; to include them.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenBalancesByAddressTest() throws ApiException, CryptoException {
        GetTokenBalancesByAddressRequest getTokenBalancesByAddressRequest = new GetTokenBalancesByAddressRequest();
        getTokenBalancesByAddressRequest.address("0x50c476a139aab23fdaf9bca12614cdd54a4244e3");

        GetTokenBalancesByAddressRequestTokenContractAddressesInner tokenContractAddresses = new GetTokenBalancesByAddressRequestTokenContractAddressesInner();
        tokenContractAddresses.binanceChainId("1").tokenContractAddress("0x2260fac5e5542a773aa44fbcfedf7c193bc2c599");

        ArrayList<GetTokenBalancesByAddressRequestTokenContractAddressesInner> tokenContractAddressesList = new ArrayList<>();
        tokenContractAddressesList.add(tokenContractAddresses);

        getTokenBalancesByAddressRequest.tokenContractAddresses(tokenContractAddressesList);

        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        ApiResponse<GetTokenBalancesByAddressResponse> response = api.getTokenBalancesByAddress(getTokenBalancesByAddressRequest, recvWindow, nonce);


        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "7d9d5a78a341ecd7ec35e884de59ef908d4d8efbe8e266d775253f546c1cb8d7|" + actualRequest.url().queryParameter("signature"));
        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "/api/v1/dex/balance/token-balances-by-address|" + actualRequest.url().encodedPath());
        assertEquals("timestamp=1736393892000tokenContractAddresses=%5B%7B%22binanceChainId%22%3A%221%22%2C%22tokenContractAddress%22%3A%220x2260fac5e5542a773aa44fbcfedf7c193bc2c599%22%7D%5D&address=0x50c476a139aab23fdaf9bca12614cdd54a4244e3", signInputCaptor.getValue());
        assertEquals("7d9d5a78a341ecd7ec35e884de59ef908d4d8efbe8e266d775253f546c1cb8d7", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/balance/token-balances-by-address", actualRequest.url().encodedPath());
    }

    /**
     * Get Transaction Detail by Hash
     * <p>
     * Look up the full on-chain transaction detail by &#x60;binanceChainId&#x60; + &#x60;txHash&#x60;. Returns one or more entries describing transaction inputs, outputs, internal calls, and token transfers.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTransactionDetailByHashTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String binanceChainId = "1";
        String txHash = "0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890";
        String itype = "0";

        WalletApi.GetTransactionDetailByHashRequest request = new WalletApi.GetTransactionDetailByHashRequest();
        ApiResponse<GetTransactionDetailByHashResponse> response = api.getTransactionDetailByHash(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c|" + actualRequest.url().queryParameter("signature"));
        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "/api/v1/dex/post-transaction/transaction-detail-by-txhash|" + actualRequest.url().encodedPath());
        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/post-transaction/transaction-detail-by-txhash", actualRequest.url().encodedPath());
    }

    /**
     * Get Transactions by Address
     * <p>
     * Return on-chain transaction history for a wallet address across one or more chains. Results are limited to the most recent 6 months and sorted by time (descending). Supports cursor pagination, time-range filtering, and token-contract filtering.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTransactionsByAddressTest() throws ApiException, CryptoException {
        String address = "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045";
        String chains = "1,56";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String tokenContractAddress = "0xdAC17F958D2ee523a2206206994597C13D831ec7";
        Long begin = 1746000000000L;
        Long end = 1748601600000L;
        String cursor = "eyJ0aW1lIjoxNzQ4NjAwMDAwMDAwLCJpZCI6MTIzfQ==";
        Integer limit = 20;

        WalletApi.GetTransactionsByAddressRequest request = new WalletApi.GetTransactionsByAddressRequest(address, chains);
        ApiResponse<GetTransactionsByAddressResponse> response = api.getTransactionsByAddress(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "c0a375402fd67e2747587e85bf8e613b4e3676aef99298bcecb2e6078ddf486e|" + actualRequest.url().queryParameter("signature"));
        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "/api/v1/dex/post-transaction/transactions-by-address|" + actualRequest.url().encodedPath());
        assertEquals("address=0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045&chains=1%2C56&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("c0a375402fd67e2747587e85bf8e613b4e3676aef99298bcecb2e6078ddf486e", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/post-transaction/transactions-by-address", actualRequest.url().encodedPath());
    }

    /**
     * Get Wallet Supported Chains
     * <p>
     * Return blockchain networks for which the Wallet service can return balances. The supported list is dynamically configured server-side and may change over time. Pass &#x60;binanceChainId&#x60; to filter to a single chain; omit to get the full list.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getWalletSupportedChainsTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String binanceChainId = "1";

        WalletApi.GetWalletSupportedChainsRequest request = new WalletApi.GetWalletSupportedChainsRequest();
        ApiResponse<GetWalletSupportedChainsResponse> response = api.getWalletSupportedChains(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c|" + actualRequest.url().queryParameter("signature"));
        System.out.println("@@@%" + WalletApiTest.class.getCanonicalName() + "/api/v1/dex/balance/supported/chain|" + actualRequest.url().encodedPath());
        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/balance/supported/chain", actualRequest.url().encodedPath());
    }

}
