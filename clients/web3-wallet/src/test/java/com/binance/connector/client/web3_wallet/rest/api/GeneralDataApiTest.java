/*
 * Binance Web3 Wallet REST API
 * Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.rest.api;

import com.binance.connector.client.web3.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.web3.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.web3_wallet.rest.model.*;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

import static org.junit.Assert.assertEquals;

/**
 * API tests for GeneralDataApi
 */
public class GeneralDataApiTest {

    private GeneralDataApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new GeneralDataApi(apiClientSpy);
    }

    /**
     * Get Candles
     *
     * <p>Return candlestick (K-line) data for a token.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getCandlesTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0xdac17f958d2ee523a2206206994597c13d831ec7";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String bar = "BAR_1s";
        Long after = 1748600000000L;
        Long before = 1748000000000L;
        Integer limit = 100;

        GeneralDataApi.GetCandlesRequest request =
                new GeneralDataApi.GetCandlesRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetCandlesResponse> response = api.getCandles(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0xdac17f958d2ee523a2206206994597c13d831ec7&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("f9d3e7584282168d48d2b699b1e264d452e3f32a2fe22b5d7fa945390252f013", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/candles", actualRequest.url().encodedPath());
    }

    /**
     * Get Holders Ranking
     *
     * <p>Return the holders ranking list for a token, including holding amount, holding percentage,
     * native-token balance, average buy/sell price, realized PnL, and funding source. Up to 100
     * records. Pagination is not supported.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getHoldersRankingTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0x6982508145454ce325ddbe47a25d4ec3d2311933";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        Integer tagFilter = null;

        GeneralDataApi.GetHoldersRankingRequest request =
                new GeneralDataApi.GetHoldersRankingRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetHoldersRankingResponse> response = api.getHoldersRanking(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0x6982508145454ce325ddbe47a25d4ec3d2311933&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "0f84eedfb4df64482de388d2dc71e114aaccd69cb033ae6541f5278add5dc7a9", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/holder", actualRequest.url().encodedPath());
    }

    /**
     * Get Hot Token List
     *
     * <p>Get hot token ranking list. Supports sorting by volume, price change, market cap, etc.,
     * with filters for holding distribution, developer behavior, liquidity, and more.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getHotTokenListTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String binanceChainId = "1";
        Integer rankBy = null;
        Integer rankingTimeFrame = null;
        String priceChangePercentMin = "5";
        String priceChangePercentMax = "100";
        String volumeMin = "10000";
        String volumeMax = "1000000";
        String txsMin = "100";
        String txsMax = "10000";
        String marketCapMin = "100000";
        String marketCapMax = "100000000";
        String liquidityMin = "10000";
        String liquidityMax = "10000000";
        String devHoldingPercentMin = "0";
        String devHoldingPercentMax = "10";
        String inflowUsdMin = "0";
        String inflowUsdMax = "1000000";
        String holdersMin = "100";
        String holdersMax = "100000";
        String bnHolderCountMin = "0";
        String bnHolderCountMax = "10000";
        String top10HoldingPercentMin = "0";
        String top10HoldingPercentMax = "100";
        String sniperHoldingPercentMin = "0";
        String sniperHoldingPercentMax = "100";
        String smartMoneyHoldingPercentMin = "0";
        String smartMoneyHoldingPercentMax = "100";
        String kolHoldingPercentMin = "0";
        String kolHoldingPercentMax = "100";
        String proHoldingPercentMin = "0";
        String proHoldingPercentMax = "100";
        String freshWalletHoldingPercentMin = "0";
        String freshWalletHoldingPercentMax = "100";
        String insiderHoldingPercentMin = "0";
        String insiderHoldingPercentMax = "100";
        String bundlerHoldingPercentMin = "0";
        String bundlerHoldingPercentMax = "100";
        String devCreatedTokenCountMin = "0";
        String devCreatedTokenCountMax = "100";
        String devMigratedTokenCountMin = "0";
        String devMigratedTokenCountMax = "100";
        String devMigratedTokenPercentMin = "0";
        String devMigratedTokenPercentMax = "100";
        Boolean isDevSoldAll = false;
        Boolean isDevBurned = false;
        Boolean isMint = false;
        Boolean isFreeze = false;
        Boolean isHideWashTradingTokens = true;
        Boolean isHideDevWashTradingTokens = true;
        Boolean isHideInternalWashTradingTokens = true;
        Integer pageId = 1;
        Integer size = 100;

        GeneralDataApi.GetHotTokenListRequest request = new GeneralDataApi.GetHotTokenListRequest("abc123");
        ApiResponse<GetHotTokenListResponse> response = api.getHotTokenList(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=abc123&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "7b51156be298e64c0d4a145bdaf590fda8263cc7df3faa0699c3a28be4a4a143", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/hot-token", actualRequest.url().encodedPath());
    }

    /**
     * Get Supported Chains
     *
     * <p>Return the list of blockchains supported by the market service.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getSupportedChainsTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetSupportedChainsRequest request = new GeneralDataApi.GetSupportedChainsRequest();
        ApiResponse<GetSupportedChainsResponse> response = api.getSupportedChains(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/supported/chain", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Advanced Info
     *
     * <p>Return advanced metrics for a token: creation info, developer and holder distribution,
     * smart money / KOL / sniper / bundler / fresh wallet holding percentages, and token tags.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenAdvancedInfoTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0x6982508145454ce325ddbe47a25d4ec3d2311933";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetTokenAdvancedInfoRequest request =
                new GeneralDataApi.GetTokenAdvancedInfoRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetTokenAdvancedInfoResponse> response = api.getTokenAdvancedInfo(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0x6982508145454ce325ddbe47a25d4ec3d2311933&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "0f84eedfb4df64482de388d2dc71e114aaccd69cb033ae6541f5278add5dc7a9",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/advanced-info", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Basic Info
     *
     * <p>Return basic metadata for a token: name, symbol, logo, decimals, creator address, creation
     * time, and tag info.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenBasicInfoTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0xdac17f958d2ee523a2206206994597c13d831ec7";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetTokenBasicInfoRequest request =
                new GeneralDataApi.GetTokenBasicInfoRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetTokenBasicInfoResponse> response = api.getTokenBasicInfo(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0xdac17f958d2ee523a2206206994597c13d831ec7&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "f9d3e7584282168d48d2b699b1e264d452e3f32a2fe22b5d7fa945390252f013", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/basic-info", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Price
     *
     * <p>Get the latest price for tokens. Supports batch queries, up to 100 tokens per request.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenPriceTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetTokenPriceRequest request = new GeneralDataApi.GetTokenPriceRequest();
        ApiResponse<GetTokenPriceResponse> response = api.getTokenPrice(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/price", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Trades
     *
     * <p>Return on-chain trade history for a token. Supports tag filtering and wallet address
     * filtering.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenTradesTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0x6982508145454ce325ddbe47a25d4ec3d2311933";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String cursor = "eyJ0aW1lIjoxNzQ4NjAwMDAwMDAwLCJpZCI6MTIzfQ==";
        Integer limit = 100;
        Integer tagFilter = null;
        String walletAddressFilter = "0x28c6c06298d514db089934071355e5743bf21d60";

        GeneralDataApi.GetTokenTradesRequest request =
                new GeneralDataApi.GetTokenTradesRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetTokenTradesResponse> response = api.getTokenTrades(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0x6982508145454ce325ddbe47a25d4ec3d2311933&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("0f84eedfb4df64482de388d2dc71e114aaccd69cb033ae6541f5278add5dc7a9", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/trades", actualRequest.url().encodedPath());
    }

    /**
     * Get Token Trading Info
     *
     * <p>Get token price and trading data (volume, transactions, market cap, holders, etc.).
     * Supports batch queries for up to 100 tokens.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTokenTradingInfoTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetTokenTradingInfoRequest request = new GeneralDataApi.GetTokenTradingInfoRequest();
        ApiResponse<GetTokenTradingInfoResponse> response = api.getTokenTradingInfo(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/price-info", actualRequest.url().encodedPath());
    }

    /**
     * Get Top Liquidity Pools
     *
     * <p>Return the top liquidity pools for a token, including pool name, protocol, liquidity in
     * USD, pool address, and per-token composition.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTopLiquidityPoolsTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0x6982508145454ce325ddbe47a25d4ec3d2311933";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.GetTopLiquidityPoolsRequest request =
                new GeneralDataApi.GetTopLiquidityPoolsRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetTopLiquidityPoolsResponse> response = api.getTopLiquidityPools(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0x6982508145454ce325ddbe47a25d4ec3d2311933&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "0f84eedfb4df64482de388d2dc71e114aaccd69cb033ae6541f5278add5dc7a9",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/top-liquidity", actualRequest.url().encodedPath());
    }

    /**
     * Get Top Traders
     *
     * <p>Return the top profit-making addresses for a token, ranked by realized PnL descending.
     * Includes holding amount, average buy/sell price, realized PnL, and funding source. Up to 100
     * records. Pagination is not supported.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTopTradersTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        String tokenContractAddress = "0x6982508145454ce325ddbe47a25d4ec3d2311933";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        Integer tagFilter = null;

        GeneralDataApi.GetTopTradersRequest request =
                new GeneralDataApi.GetTopTradersRequest(binanceChainId, tokenContractAddress);
        ApiResponse<GetTopTradersResponse> response = api.getTopTraders(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&tokenContractAddress=0x6982508145454ce325ddbe47a25d4ec3d2311933&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("0f84eedfb4df64482de388d2dc71e114aaccd69cb033ae6541f5278add5dc7a9", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/top-trader", actualRequest.url().encodedPath());
    }

    /**
     * Search Token
     *
     * <p>Search tokens by symbol or contract address.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void searchTokenTest() throws ApiException, CryptoException {
        String chains = "1,56,CT_501";
        String search = "USDT";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        GeneralDataApi.SearchTokenRequest request = new GeneralDataApi.SearchTokenRequest(chains, search);
        ApiResponse<SearchTokenResponse> response = api.searchToken(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("chains=1%2C56%2CCT_501&search=USDT&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("5b6e845ee9ce7eb56f05413dc24b4b5cec8a242981c61cd2a40dd964326e9e47", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/market/token/search", actualRequest.url().encodedPath());
    }
}
