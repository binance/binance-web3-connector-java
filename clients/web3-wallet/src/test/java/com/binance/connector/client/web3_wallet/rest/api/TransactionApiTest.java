/*
 * Binance Web3 Wallet REST API
 * Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.binance.connector.client.web3_wallet.rest.api;

import com.binance.connector.client.web3.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.web3.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.web3_wallet.rest.model.*;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

import static org.junit.Assert.assertEquals;

/**
 * API tests for TransactionApi
 */
public class TransactionApiTest {

    private TransactionApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);


        HmacSignatureGenerator signatureGenerator = new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication = new SignatureAuthentication(signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy).when(factoryMock).getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);


        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new TransactionApi(apiClientSpy);
    }

    /**
     * Broadcast Transactions
     * <p>
     * Broadcast a client-signed transaction to the chain via the OnchainOS relay. Returns the transaction hash and an internal &#x60;orderId&#x60; you can use to track on-chain status via the post-transaction service. Optional MEV protection (EVM chains only) routes the transaction through a private mempool to mitigate front-running and sandwich attacks.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void broadcastTransactionsTest() throws ApiException, CryptoException {
        BroadcastTransactionsRequest broadcastTransactionsRequest = new BroadcastTransactionsRequest();
        broadcastTransactionsRequest.binanceChainId("1");
        broadcastTransactionsRequest.signedTransaction("0xf86c808504a817c80082520894d8da6bf26964af9d7eed9e03e53415d37aa96045880de0b6b3a76400008025a0...");
        broadcastTransactionsRequest.address("0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045");

        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        ApiResponse<BroadcastTransactionsResponse> response = api.broadcastTransactions(broadcastTransactionsRequest, recvWindow, nonce);


        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000address=0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045&signedTransaction=0xf86c808504a817c80082520894d8da6bf26964af9d7eed9e03e53415d37aa96045880de0b6b3a76400008025a0...&binanceChainId=1", signInputCaptor.getValue());
        assertEquals("66b7a8a0ef786de8ddd3f218bf9ba196221f60fbfcdb12926a2f1cbc624f84bb", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/pre-transaction/broadcast-transaction", actualRequest.url().encodedPath());
    }

    /**
     * Get Broadcast Orders
     * <p>
     * Look up broadcast orders previously submitted via &#x60;/pre-transaction/broadcast-transaction&#x60;. Filter by &#x60;txStatus&#x60; or &#x60;orderId&#x60;, paginate with &#x60;cursor&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getBroadcastOrdersTest() throws ApiException, CryptoException {
        String address = "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045";
        String binanceChainId = "1";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        String txStatus = "success";
        String orderId = "1812345678901234567";
        String cursor = "eyJ0aW1lIjoxNzQ4NjAwMDAwMDAwLCJpZCI6MTIzfQ==";
        Integer limit = 20;

        TransactionApi.GetBroadcastOrdersRequest request = new TransactionApi.GetBroadcastOrdersRequest(address, binanceChainId);
        ApiResponse<GetBroadcastOrdersResponse> response = api.getBroadcastOrders(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("address=0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045&binanceChainId=1&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("f4b74092bfc5611f8d25f362db6a4633a58f952a500f2a98c173e5f439bab6e9", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/post-transaction/orders", actualRequest.url().encodedPath());
    }

    /**
     * Get Gas Limit
     * <p>
     * Estimate the gas limit (or compute-unit ceiling on Solana) for an unsigned transaction. Provide either &#x60;evmTx&#x60; for EVM chains or &#x60;solTx&#x60; for Solana, matching the value of &#x60;binanceChainId&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getGasLimitTest() throws ApiException, CryptoException {
        GetGasLimitRequest getGasLimitRequest = new GetGasLimitRequest();
        getGasLimitRequest.binanceChainId("1");

        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        ApiResponse<GetGasLimitResponse> response = api.getGasLimit(getGasLimitRequest, recvWindow, nonce);


        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000binanceChainId=1", signInputCaptor.getValue());
        assertEquals("0708456ff0bebe4504a390eba1cdb4a23e3d7efc004bbc03345597b275ce0a21", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/pre-transaction/gas-limit", actualRequest.url().encodedPath());
    }

    /**
     * Get Gas Price
     * <p>
     * Query the current network gas price for the specified chain. The response shape varies by chain family: - EVM chains return both &#x60;evmLegacyGasPrice&#x60; (legacy gasPrice) and   &#x60;eip1559GasPrice&#x60; (baseFee + priority/max fees) when EIP-1559 is supported. - Solana returns &#x60;solanaGasPrice&#x60; (compute-unit prices and Jito tips). Fields not applicable to the chain family are returned as &#x60;null&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getGasPriceTest() throws ApiException, CryptoException {
        String binanceChainId = "1";
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        TransactionApi.GetGasPriceRequest request = new TransactionApi.GetGasPriceRequest(binanceChainId);
        ApiResponse<GetGasPriceResponse> response = api.getGasPrice(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("binanceChainId=1&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("c36923d9a327f1bd1ee2057e16c68ce0db69c823fc0df9cff959266fc9bebce6", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/pre-transaction/gas-price", actualRequest.url().encodedPath());
    }

    /**
     * Get Transaction Supported Chains
     * <p>
     * Return the blockchain networks supported by the Transaction service for gas estimation, simulation, and broadcasting. The list is dynamically configured server-side and may change over time.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getTransactionSupportedChainsTest() throws ApiException, CryptoException {
        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";

        TransactionApi.GetTransactionSupportedChainsRequest request = new TransactionApi.GetTransactionSupportedChainsRequest();
        ApiResponse<GetTransactionSupportedChainsResponse> response = api.getTransactionSupportedChains(request);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/pre-transaction/supported/chain", actualRequest.url().encodedPath());
    }

    /**
     * Simulate Transactions
     * <p>
     * Simulate transaction execution off-chain to predict its outcome before broadcasting. The response includes the predicted execution status, balance changes per affected account/token, and ERC-20 allowance changes (EVM chains). Provide either &#x60;evmTx&#x60; (EVM chains) or &#x60;solTx&#x60; (Solana) matching &#x60;binanceChainId&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void simulateTransactionsTest() throws ApiException, CryptoException {
        SimulateTransactionsRequest simulateTransactionsRequest = new SimulateTransactionsRequest();
        simulateTransactionsRequest.binanceChainId("1");

        Long recvWindow = 5000L;
        String nonce = "unique-nonce-string";
        ApiResponse<SimulateTransactionsResponse> response = api.simulateTransactions(simulateTransactionsRequest, recvWindow, nonce);


        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy).execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());
        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000binanceChainId=1", signInputCaptor.getValue());
        assertEquals("0708456ff0bebe4504a390eba1cdb4a23e3d7efc004bbc03345597b275ce0a21", actualRequest.url().queryParameter("signature"));
        assertEquals("/api/v1/dex/pre-transaction/simulate", actualRequest.url().encodedPath());
    }

}
