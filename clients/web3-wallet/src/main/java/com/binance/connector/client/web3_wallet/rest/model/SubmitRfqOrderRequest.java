/*
 * Binance Web3 API
 * Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.rest.model;

import com.binance.connector.client.web3_wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** SubmitRfqOrderRequest */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class SubmitRfqOrderRequest {
    public static final String SERIALIZED_NAME_REQUEST_ID = "requestId";

    @SerializedName(SERIALIZED_NAME_REQUEST_ID)
    @jakarta.annotation.Nonnull
    private String requestId;

    public static final String SERIALIZED_NAME_USER_SIGNATURE = "userSignature";

    @SerializedName(SERIALIZED_NAME_USER_SIGNATURE)
    @jakarta.annotation.Nonnull
    private String userSignature;

    /**
     * RFQ vendor name — must match &#x60;rfq.vendor&#x60; from the &#x60;/swap&#x60; response. The
     * backend validates this against &#x60;quoteId&#x60; to prevent misrouted submissions.
     */
    @JsonAdapter(VendorEnum.Adapter.class)
    public enum VendorEnum {
        InchFusion("InchFusion"),

        CowSwap("CowSwap"),

        PcsXRfq("PcsXRfq");

        private String value;

        VendorEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static VendorEnum fromValue(String value) {
            for (VendorEnum b : VendorEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            throw new IllegalArgumentException("Unexpected value '" + value + "'");
        }

        public static class Adapter extends TypeAdapter<VendorEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final VendorEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public VendorEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return VendorEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            VendorEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_VENDOR = "vendor";

    @SerializedName(SERIALIZED_NAME_VENDOR)
    @jakarta.annotation.Nonnull
    private VendorEnum vendor;

    public static final String SERIALIZED_NAME_QUOTE_ID = "quoteId";

    @SerializedName(SERIALIZED_NAME_QUOTE_ID)
    @jakarta.annotation.Nonnull
    private String quoteId;

    public static final String SERIALIZED_NAME_SIGNING_SCHEME = "signingScheme";

    @SerializedName(SERIALIZED_NAME_SIGNING_SCHEME)
    @jakarta.annotation.Nullable
    private String signingScheme;

    public SubmitRfqOrderRequest() {}

    public SubmitRfqOrderRequest requestId(@jakarta.annotation.Nonnull String requestId) {
        this.requestId = requestId;
        return this;
    }

    /**
     * Idempotency key (UUID v4). Within 30 minutes, re-submitting with the same
     * &#x60;requestId&#x60; returns the original result without re-calling the vendor. Generate a
     * new UUID for each new order; reuse the same UUID when retrying the same attempt.
     *
     * @return requestId
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public String getRequestId() {
        return requestId;
    }

    public void setRequestId(@jakarta.annotation.Nonnull String requestId) {
        this.requestId = requestId;
    }

    public SubmitRfqOrderRequest userSignature(@jakarta.annotation.Nonnull String userSignature) {
        this.userSignature = userSignature;
        return this;
    }

    /**
     * EIP-712 signature of &#x60;rfq.typedDataToSign&#x60; from the &#x60;/swap&#x60; response.
     * Must be a hex string with &#x60;0x&#x60; prefix (132 chars &#x3D; &#x60;0x&#x60; + 65 bytes).
     * The signing wallet must match &#x60;userWalletAddress&#x60; used in &#x60;/quote&#x60;. The
     * backend automatically corrects Ethereum &#x60;v&#x60; values &#x60;00&#x60;/&#x60;01&#x60; to
     * &#x60;1b&#x60;/&#x60;1c&#x60;.
     *
     * @return userSignature
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public String getUserSignature() {
        return userSignature;
    }

    public void setUserSignature(@jakarta.annotation.Nonnull String userSignature) {
        this.userSignature = userSignature;
    }

    public SubmitRfqOrderRequest vendor(@jakarta.annotation.Nonnull VendorEnum vendor) {
        this.vendor = vendor;
        return this;
    }

    /**
     * RFQ vendor name — must match &#x60;rfq.vendor&#x60; from the &#x60;/swap&#x60; response. The
     * backend validates this against &#x60;quoteId&#x60; to prevent misrouted submissions.
     *
     * @return vendor
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public VendorEnum getVendor() {
        return vendor;
    }

    public void setVendor(@jakarta.annotation.Nonnull VendorEnum vendor) {
        this.vendor = vendor;
    }

    public SubmitRfqOrderRequest quoteId(@jakarta.annotation.Nonnull String quoteId) {
        this.quoteId = quoteId;
        return this;
    }

    /**
     * &#x60;rfq.orderId&#x60; from the &#x60;/swap&#x60; response. The backend uses this to look up
     * the vendor and chain context required to forward the signed order.
     *
     * @return quoteId
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public String getQuoteId() {
        return quoteId;
    }

    public void setQuoteId(@jakarta.annotation.Nonnull String quoteId) {
        this.quoteId = quoteId;
    }

    public SubmitRfqOrderRequest signingScheme(@jakarta.annotation.Nullable String signingScheme) {
        this.signingScheme = signingScheme;
        return this;
    }

    /**
     * Signing scheme from &#x60;rfq.signingScheme&#x60; in the &#x60;/swap&#x60; response. Optional
     * — when omitted the backend infers the default per vendor.
     *
     * @return signingScheme
     */
    @jakarta.annotation.Nullable
    public String getSigningScheme() {
        return signingScheme;
    }

    public void setSigningScheme(@jakarta.annotation.Nullable String signingScheme) {
        this.signingScheme = signingScheme;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SubmitRfqOrderRequest submitRfqOrderRequest = (SubmitRfqOrderRequest) o;
        return Objects.equals(this.requestId, submitRfqOrderRequest.requestId)
                && Objects.equals(this.userSignature, submitRfqOrderRequest.userSignature)
                && Objects.equals(this.vendor, submitRfqOrderRequest.vendor)
                && Objects.equals(this.quoteId, submitRfqOrderRequest.quoteId)
                && Objects.equals(this.signingScheme, submitRfqOrderRequest.signingScheme);
    }

    @Override
    public int hashCode() {
        return Objects.hash(requestId, userSignature, vendor, quoteId, signingScheme);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SubmitRfqOrderRequest {\n");
        sb.append("		requestId: ").append(toIndentedString(requestId)).append("\n");
        sb.append("		userSignature: ").append(toIndentedString(userSignature)).append("\n");
        sb.append("		vendor: ").append(toIndentedString(vendor)).append("\n");
        sb.append("		quoteId: ").append(toIndentedString(quoteId)).append("\n");
        sb.append("		signingScheme: ").append(toIndentedString(signingScheme)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object requestIdValue = getRequestId();
        String requestIdValueAsString = "";
        requestIdValueAsString = requestIdValue.toString();
        sb.append("requestId=").append(urlEncode(requestIdValueAsString)).append("");
        Object userSignatureValue = getUserSignature();
        String userSignatureValueAsString = "";
        userSignatureValueAsString = userSignatureValue.toString();
        sb.append("userSignature=").append(urlEncode(userSignatureValueAsString)).append("");
        Object vendorValue = getVendor();
        String vendorValueAsString = "";
        vendorValueAsString = vendorValue.toString();
        sb.append("vendor=").append(urlEncode(vendorValueAsString)).append("");
        Object quoteIdValue = getQuoteId();
        String quoteIdValueAsString = "";
        quoteIdValueAsString = quoteIdValue.toString();
        sb.append("quoteId=").append(urlEncode(quoteIdValueAsString)).append("");
        Object signingSchemeValue = getSigningScheme();
        String signingSchemeValueAsString = "";
        signingSchemeValueAsString = signingSchemeValue.toString();
        sb.append("signingScheme=").append(urlEncode(signingSchemeValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("requestId");
        openapiFields.add("userSignature");
        openapiFields.add("vendor");
        openapiFields.add("quoteId");
        openapiFields.add("signingScheme");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("requestId");
        openapiRequiredFields.add("userSignature");
        openapiRequiredFields.add("vendor");
        openapiRequiredFields.add("quoteId");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SubmitRfqOrderRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SubmitRfqOrderRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in SubmitRfqOrderRequest is not found in"
                                        + " the empty JSON string",
                                SubmitRfqOrderRequest.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : SubmitRfqOrderRequest.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("requestId").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `requestId` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("requestId").toString()));
        }
        if (!jsonObj.get("userSignature").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `userSignature` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("userSignature").toString()));
        }
        if (!jsonObj.get("vendor").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `vendor` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("vendor").toString()));
        }
        // validate the required field `vendor`
        VendorEnum.validateJsonElement(jsonObj.get("vendor"));
        if (!jsonObj.get("quoteId").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `quoteId` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("quoteId").toString()));
        }
        if ((jsonObj.get("signingScheme") != null && !jsonObj.get("signingScheme").isJsonNull())
                && !jsonObj.get("signingScheme").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `signingScheme` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("signingScheme").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SubmitRfqOrderRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SubmitRfqOrderRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SubmitRfqOrderRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SubmitRfqOrderRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SubmitRfqOrderRequest>() {
                        @Override
                        public void write(JsonWriter out, SubmitRfqOrderRequest value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SubmitRfqOrderRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SubmitRfqOrderRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SubmitRfqOrderRequest
     * @throws IOException if the JSON string is invalid with respect to SubmitRfqOrderRequest
     */
    public static SubmitRfqOrderRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SubmitRfqOrderRequest.class);
    }

    /**
     * Convert an instance of SubmitRfqOrderRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
