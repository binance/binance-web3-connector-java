/*
 * Binance Web3 WebSocket Streams
 * Real-time on-chain data streaming over WebSocket for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.websocket.stream.api;

import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.SystemUtil;
import com.binance.connector.client.common.exception.ConstraintViolationException;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionInterface;
import com.binance.connector.client.common.websocket.dtos.RequestWrapperDTO;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueue;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueueWrapper;
import com.binance.connector.client.web3.common.websocket.adapter.stream.Web3StreamConnectionPoolWrapper;
import com.binance.connector.client.web3.common.websocket.adapter.stream.Web3StreamConnectionWrapper;
import com.binance.connector.client.web3.common.websocket.configuration.Web3WebSocketClientConfiguration;
import com.binance.connector.client.web3_wallet.websocket.stream.JSON;
import com.binance.connector.client.web3_wallet.websocket.stream.model.CandlestickStreamRequest;
import com.binance.connector.client.web3_wallet.websocket.stream.model.CandlestickStreamResponse;
import com.binance.connector.client.web3_wallet.websocket.stream.model.MarketStatsStreamRequest;
import com.binance.connector.client.web3_wallet.websocket.stream.model.MarketStatsStreamResponse;
import com.binance.connector.client.web3_wallet.websocket.stream.model.PriceStreamRequest;
import com.binance.connector.client.web3_wallet.websocket.stream.model.PriceStreamResponse;
import com.binance.connector.client.web3_wallet.websocket.stream.model.RealTimeTradesStreamRequest;
import com.binance.connector.client.web3_wallet.websocket.stream.model.RealTimeTradesStreamResponse;
import com.google.gson.reflect.TypeToken;
import jakarta.validation.ConstraintViolation;
import jakarta.validation.Validation;
import jakarta.validation.Validator;
import jakarta.validation.constraints.*;
import java.util.Collections;
import java.util.Map;
import java.util.Set;
import java.util.UUID;
import org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator;

public class MarketApi {
    private static final String USER_AGENT =
            String.format(
                    "binance-web3-wallet/5.0.0 (Java/%s; %s; %s)",
                    SystemUtil.getJavaVersion(), SystemUtil.getOs(), SystemUtil.getArch());

    private StreamConnectionInterface connection;

    public MarketApi() {}

    public MarketApi(Web3WebSocketClientConfiguration configuration) {
        this(
                configuration.getUsePool()
                        ? new Web3StreamConnectionPoolWrapper(configuration, JSON.getGson())
                        : new Web3StreamConnectionWrapper(configuration, JSON.getGson()));
    }

    public MarketApi(StreamConnectionInterface connection) {
        connection.setUserAgent(USER_AGENT);
        if (!connection.isConnected()) {
            connection.connect();
        }
        this.connection = connection;
    }

    public StreamConnectionInterface getConnection() {
        return connection;
    }

    /**
     * Candlestick Stream Push token candlestick (K-line) data in real time. The current (unclosed)
     * candle keeps updating as new trades occur. Update Speed: Real-time
     *
     * @param candlestickStreamRequest (required)
     * @return CandlestickStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Candles stream payload. </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/ws-streams/market#candlestick-stream">Candlestick
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<CandlestickStreamResponse> candlestickStream(
            CandlestickStreamRequest candlestickStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = candlestickStreamRaw(candlestickStreamRequest);

        TypeToken<CandlestickStreamResponse> typeToken =
                new TypeToken<CandlestickStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> candlestickStreamRaw(
            CandlestickStreamRequest candlestickStreamRequest) throws ApiException {
        candlestickStreamValidateBeforeCall(candlestickStreamRequest);

        String methodName =
                "/w3w@onchainos@pub@candles@<bar>@<chainId>@<contractAddress>"
                        .substring(1)
                        .replace(
                                "<id>",
                                candlestickStreamRequest.getId() != null
                                        ? candlestickStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<bar>",
                                candlestickStreamRequest.getBar() != null
                                        ? candlestickStreamRequest.getBar().toString()
                                        : "")
                        .replace(
                                "<chainId>",
                                candlestickStreamRequest.getChainId() != null
                                        ? candlestickStreamRequest.getChainId().toString()
                                        : "")
                        .replace(
                                "<contractAddress>",
                                candlestickStreamRequest.getContractAddress() != null
                                        ? candlestickStreamRequest.getContractAddress().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void candlestickStreamValidateBeforeCall(
            CandlestickStreamRequest candlestickStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<CandlestickStreamRequest>> violations =
                    validator.validate(candlestickStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Market Stats Stream Push token market data. Rate-limited to at most once per second per
     * token: if a token&#39;s data changes within a 1-second window, the latest snapshot is pushed
     * once (multiple changes are coalesced into the latest value); if there is no change, nothing
     * is pushed. Update Speed: Real-time
     *
     * @param marketStatsStreamRequest (required)
     * @return MarketStatsStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> TX data stream payload. </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/ws-streams/market#market-stats-stream">Market
     *     Stats Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<MarketStatsStreamResponse> marketStatsStream(
            MarketStatsStreamRequest marketStatsStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = marketStatsStreamRaw(marketStatsStreamRequest);

        TypeToken<MarketStatsStreamResponse> typeToken =
                new TypeToken<MarketStatsStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> marketStatsStreamRaw(
            MarketStatsStreamRequest marketStatsStreamRequest) throws ApiException {
        marketStatsStreamValidateBeforeCall(marketStatsStreamRequest);

        String methodName =
                "/w3w@onchainos@pub@tx-data@<chainId>@<contractAddress>"
                        .substring(1)
                        .replace(
                                "<id>",
                                marketStatsStreamRequest.getId() != null
                                        ? marketStatsStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<chainId>",
                                marketStatsStreamRequest.getChainId() != null
                                        ? marketStatsStreamRequest.getChainId().toString()
                                        : "")
                        .replace(
                                "<contractAddress>",
                                marketStatsStreamRequest.getContractAddress() != null
                                        ? marketStatsStreamRequest.getContractAddress().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void marketStatsStreamValidateBeforeCall(
            MarketStatsStreamRequest marketStatsStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<MarketStatsStreamRequest>> violations =
                    validator.validate(marketStatsStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Price Stream Push token price updates. Triggered every time a token&#39;s price changes.
     * Update Speed: Real-time
     *
     * @param priceStreamRequest (required)
     * @return PriceStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Price stream payload. </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/ws-streams/market#price-stream">Price
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<PriceStreamResponse> priceStream(
            PriceStreamRequest priceStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = priceStreamRaw(priceStreamRequest);

        TypeToken<PriceStreamResponse> typeToken = new TypeToken<PriceStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> priceStreamRaw(PriceStreamRequest priceStreamRequest)
            throws ApiException {
        priceStreamValidateBeforeCall(priceStreamRequest);

        String methodName =
                "/w3w@onchainos@pub@price@<chainId>@<contractAddress>"
                        .substring(1)
                        .replace(
                                "<id>",
                                priceStreamRequest.getId() != null
                                        ? priceStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<chainId>",
                                priceStreamRequest.getChainId() != null
                                        ? priceStreamRequest.getChainId().toString()
                                        : "")
                        .replace(
                                "<contractAddress>",
                                priceStreamRequest.getContractAddress() != null
                                        ? priceStreamRequest.getContractAddress().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void priceStreamValidateBeforeCall(PriceStreamRequest priceStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<PriceStreamRequest>> violations =
                    validator.validate(priceStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Real-Time Trades Stream Push real-time token trade history. Triggered on every new
     * transaction. Update Speed: Real-time
     *
     * @param realTimeTradesStreamRequest (required)
     * @return RealTimeTradesStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Trade history stream payload. </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/ws-streams/market#real-time-trades-stream">Real-Time
     *     Trades Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<RealTimeTradesStreamResponse> realTimeTradesStream(
            RealTimeTradesStreamRequest realTimeTradesStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = realTimeTradesStreamRaw(realTimeTradesStreamRequest);

        TypeToken<RealTimeTradesStreamResponse> typeToken =
                new TypeToken<RealTimeTradesStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> realTimeTradesStreamRaw(
            RealTimeTradesStreamRequest realTimeTradesStreamRequest) throws ApiException {
        realTimeTradesStreamValidateBeforeCall(realTimeTradesStreamRequest);

        String methodName =
                "/w3w@onchainos@pub@tx@<chainId>@<contractAddress>"
                        .substring(1)
                        .replace(
                                "<id>",
                                realTimeTradesStreamRequest.getId() != null
                                        ? realTimeTradesStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<chainId>",
                                realTimeTradesStreamRequest.getChainId() != null
                                        ? realTimeTradesStreamRequest.getChainId().toString()
                                        : "")
                        .replace(
                                "<contractAddress>",
                                realTimeTradesStreamRequest.getContractAddress() != null
                                        ? realTimeTradesStreamRequest
                                                .getContractAddress()
                                                .toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void realTimeTradesStreamValidateBeforeCall(
            RealTimeTradesStreamRequest realTimeTradesStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<RealTimeTradesStreamRequest>> violations =
                    validator.validate(realTimeTradesStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    public String getRequestID() {
        return UUID.randomUUID().toString();
    }
}
