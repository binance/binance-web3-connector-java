/*
 * Binance Web3 API
 * Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.web3_wallet.rest.model;

import com.binance.connector.client.web3_wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * Lending health-factor change. Contains &#x60;before&#x60; / &#x60;after&#x60; values for lending
 * protocols (e.g. Aave / Venus); &#x60;null&#x60; for non-lending protocols or when health factor
 * is unavailable.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class DefiPreviewHealthFactor {
    public static final String SERIALIZED_NAME_BEFORE = "before";

    @SerializedName(SERIALIZED_NAME_BEFORE)
    @jakarta.annotation.Nullable
    private String before;

    public static final String SERIALIZED_NAME_AFTER = "after";

    @SerializedName(SERIALIZED_NAME_AFTER)
    @jakarta.annotation.Nullable
    private String after;

    public DefiPreviewHealthFactor() {}

    public DefiPreviewHealthFactor before(@jakarta.annotation.Nullable String before) {
        this.before = before;
        return this;
    }

    /**
     * Health factor before this transaction. Null when unavailable.
     *
     * @return before
     */
    @jakarta.annotation.Nullable
    public String getBefore() {
        return before;
    }

    public void setBefore(@jakarta.annotation.Nullable String before) {
        this.before = before;
    }

    public DefiPreviewHealthFactor after(@jakarta.annotation.Nullable String after) {
        this.after = after;
        return this;
    }

    /**
     * Health factor after this transaction. Null when unavailable.
     *
     * @return after
     */
    @jakarta.annotation.Nullable
    public String getAfter() {
        return after;
    }

    public void setAfter(@jakarta.annotation.Nullable String after) {
        this.after = after;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DefiPreviewHealthFactor defiPreviewHealthFactor = (DefiPreviewHealthFactor) o;
        return Objects.equals(this.before, defiPreviewHealthFactor.before)
                && Objects.equals(this.after, defiPreviewHealthFactor.after);
    }

    private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
        return a == b
                || (a != null
                        && b != null
                        && a.isPresent()
                        && b.isPresent()
                        && Objects.deepEquals(a.get(), b.get()));
    }

    @Override
    public int hashCode() {
        return Objects.hash(before, after);
    }

    private static <T> int hashCodeNullable(JsonNullable<T> a) {
        if (a == null) {
            return 1;
        }
        return a.isPresent() ? Arrays.deepHashCode(new Object[] {a.get()}) : 31;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class DefiPreviewHealthFactor {\n");
        sb.append("		before: ").append(toIndentedString(before)).append("\n");
        sb.append("		after: ").append(toIndentedString(after)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object beforeValue = getBefore();
        String beforeValueAsString = "";
        beforeValueAsString = beforeValue.toString();
        sb.append("before=").append(urlEncode(beforeValueAsString)).append("");
        Object afterValue = getAfter();
        String afterValueAsString = "";
        afterValueAsString = afterValue.toString();
        sb.append("after=").append(urlEncode(afterValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("before");
        openapiFields.add("after");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to DefiPreviewHealthFactor
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!DefiPreviewHealthFactor.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in DefiPreviewHealthFactor is not found"
                                        + " in the empty JSON string",
                                DefiPreviewHealthFactor.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("before") != null && !jsonObj.get("before").isJsonNull())
                && !jsonObj.get("before").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `before` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("before").toString()));
        }
        if ((jsonObj.get("after") != null && !jsonObj.get("after").isJsonNull())
                && !jsonObj.get("after").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `after` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("after").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!DefiPreviewHealthFactor.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'DefiPreviewHealthFactor' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<DefiPreviewHealthFactor> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(DefiPreviewHealthFactor.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<DefiPreviewHealthFactor>() {
                        @Override
                        public void write(JsonWriter out, DefiPreviewHealthFactor value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public DefiPreviewHealthFactor read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of DefiPreviewHealthFactor given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of DefiPreviewHealthFactor
     * @throws IOException if the JSON string is invalid with respect to DefiPreviewHealthFactor
     */
    public static DefiPreviewHealthFactor fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, DefiPreviewHealthFactor.class);
    }

    /**
     * Convert an instance of DefiPreviewHealthFactor to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
